# Blueprint Render — https://render.com/docs/blueprint-spec
# Telegram: JOANA_MESSENGER=telegram + TELEGRAM_BOT_TOKEN (ver TELEGRAM-SETUP.md)
# WhatsApp Meta: callback https://.../webhook

services:
  - type: web
    name: joana
    env: node
    region: frankfurt
    plan: free
    buildCommand: npm install
    startCommand: npm run start:prod
    healthCheckPath: /health
